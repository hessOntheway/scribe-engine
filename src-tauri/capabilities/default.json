{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Scribe Engine window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
